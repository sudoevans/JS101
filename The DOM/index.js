function calculate_area(side){
    side=document.getElementById("lenght").value;
    var area=side*side; //basic calculation of area of square.
    // document.getElementById("result").textContent="Area: "+area;
    alert(area);
}
    